# Marionette.Toolkit.App

`Marionette.Toolkit.App` is an extention of `StateClass`.  Its purpose is to provide an object with a `initialize`/`start`/`stop`/`destroy` lifecycle.  All events bound to the `AbstractApp` while running (and only those) will be removed when stopped.
